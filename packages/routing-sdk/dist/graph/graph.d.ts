import { Pool, Graph } from '../types';
type GraphParams = {
    pools?: Pool[];
    graph?: Graph;
};
export declare function createGraph({ pools, graph }: GraphParams): Graph;
export {};
//# sourceMappingURL=graph.d.ts.map